// The web layout's palette. These are the app's existing theme colours (see the
// `theme` object in App.js) - same values, named for how the desktop layout uses
// them. Nothing new was introduced; the redesign is layout only.
export const BROWN = '#5A4230';      // theme.colors.primary
export const ORANGE = '#D86A3A';     // theme.colors.secondary
export const YELLOW = '#F2BC42';     // theme.colors.tertiary
export const SAND = '#F0DCA0';       // theme.colors.background
export const CREAM = '#f7f0e2';      // theme.colors.surface
export const NAVY = '#2C3E50';       // theme.colors.accent
export const ERROR = '#d63031';      // theme.colors.error

// Darker brown used for the page background / nav bar. Same hue family as BROWN,
// shifted down for contrast against the cream content area.
export const PAGE_BG = '#3D2E1F';

export const INK = '#3C2F1A';        // Body text on light surfaces
export const MUTED = '#8A7355';      // Secondary text
